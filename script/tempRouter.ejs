'use strict';

const <%=table%>Service = require(rootPath.concat("/service/<%=table%>Service.js"));
const <%=table%>Router = express.Router();

/*
 * title : 路由1
 * url : /api/test
 * method : get
 * code: 1 操作成功
 * code: 0 操作失败
 * return :
 * ``` 
 * {
 *   hello:'world',
 * }
 * ```
 * */
<%=table%>Router.get("/",<%=table%>Service.select);

/*
 * title : 路由1
 * url : /api/test
 * method : get
 * params : id path true 用户名
 * code: 1 操作成功
 * code: 0 操作失败
 * return :
 * ``` 
 * {
 *   hello:'world',
 * }
 * ```
 * */
<%=table%>Router.get("/:id",<%=table%>Service.get);

/*
 * title : 路由1
 * url : /api/test
 * method : post
 * params : name body true 用户名
 * code: 1 操作成功
 * code: 0 操作失败
 * */
<%=table%>Router.post("/",<%=table%>Service.add);

/*
 * title : 路由1
 * url : /api/test
 * method : put
 * params : id path true 主键
 * code: 1 操作成功
 * code: 0 操作失败
 * */
<%=table%>Router.put("/:id",<%=table%>Service.update);

/*
 * title : 路由1
 * url : /api/test
 * method : delete
 * params : id path true 主键
 * code: 1 操作成功
 * code: 0 操作失败
 * */
<%=table%>Router.delete("/:id",<%=table%>Service.delete);

module.exports = <%=table%>Router;